node_modules
.DS_Store
# Add ignore for fixtures generated by test at random for each test. 
# These files however are still committed to code repository for reference
# to ignore changes in these files after clone, run the following command:
# git update-index --assume-unchanged cypress/fixtures/userhappy.Data.json
# git update-index --assume-unchanged cypress/fixtures/userunhappyWordCount.Data.json
*.Data.json